# requirements.txt
#
# The current prototype (index.html) is fully client-side:
#   - No Python backend, no pip packages, no API keys required.
#   - STT runs via the browser's built-in Web Speech API.
#   - Evaluation runs via in-browser JS (keyword-coverage rubric).
#
# Nothing below is required to run index.html. These are listed only
# for the OPTIONAL server-side extension path described in README.md
# section 6 (swap-in LLM grader / server-side STT). Install only if
# you build that extension.

# --- Optional: lightweight local server to serve index.html (CORS-safe mic access) ---
# python -m http.server already ships with Python, no install needed.

# --- Optional: server-side STT instead of browser Web Speech API ---
# SpeechRecognition==3.10.4
# openai-whisper==20231117

# --- Optional: LLM-based answer grading instead of keyword rubric ---
# anthropic==0.40.0

# --- Optional: simple backend if you move evaluation server-side ---
# flask==3.0.3